# Evidências

As capturas reais são produzidas durante a execução do Playwright e anexadas ao relatório HTML.

```bash
npm run test:web
npm run report:web
```

Os arquivos serão gravados em `relatorios/test-results/` e exibidos em `relatorios/playwright-report/`.
